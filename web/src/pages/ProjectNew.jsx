// Placeholder — built next, per the spec's page order (/projects/new).
export function ProjectNew() {
  return (
    <div className="page">
      <p className="muted">Add project — coming next.</p>
    </div>
  );
}
