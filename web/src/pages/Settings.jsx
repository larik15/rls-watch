// Placeholder — built last among the pages, per the spec's page order.
export function Settings() {
  return (
    <div className="page">
      <p className="muted">Settings — coming soon.</p>
    </div>
  );
}
